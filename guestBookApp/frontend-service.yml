apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    name: frontend-service
    app: guest-book-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80

  selector:
    name: frontend-pod
    app: guest-book-app
